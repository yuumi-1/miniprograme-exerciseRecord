<!--pages/trainCalendar/trainCalendar.wxml-->
<view class="page">
  <t-navbar title="训练日历" left-arrow />
  <van-calendar show="{{show}}"
  poppable="{{false}}"
  show-title="{{false}}"
  show-subtitle="{{false}}"
  first-day-of-week="{{ 1 }}"
  formatter="{{ formatter }}"
  />
</view>